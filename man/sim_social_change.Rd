% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_social_change}
\alias{sim_social_change}
\title{Simulate social change with demographic processes}
\usage{
sim_social_change(
  periods,
  data,
  fun_y,
  fun_coming_of_age = NULL,
  fun_mortality = NULL,
  fun_inmigration = NULL,
  fun_outmigration = NULL,
  fun_transitions = NULL
)
}
\arguments{
\item{periods}{Integer number of time periods to simulate}

\item{data}{Initial population data.table with columns \code{age}, \code{n}, and optional covariates}

\item{fun_y}{Outcome function taking \code{(data, time)} and returning outcome values}

\item{fun_coming_of_age}{Optional function taking \code{(data, period)} returning new entrants data.table}

\item{fun_mortality}{Optional function taking \code{(data, period)} returning mortality counts per cell}

\item{fun_inmigration}{Optional function taking \code{(data, period)} returning immigrants data.table}

\item{fun_outmigration}{Optional function taking \code{(data, period)} returning out-migration counts per cell}

\item{fun_transitions}{Optional function taking \code{(data, period)} returning state transitions data.table}
}
\value{
S3 object of class \code{social_change_sim} with components:
  \itemize{
    \item \code{summary}: data.table summarizing change components by period
    \item \code{snapshot}: list of population snapshots at each period
    \item \code{record}: list of event-by-event change records
  }
}
\description{
Forward simulation of social change dynamics, decomposing aggregate change into
components driven by intraindividual change and population turnover (mortality,
coming-of-age, migration, and state transitions).
}
\examples{
\dontrun{
library(data.table)
data <- data.table(age = 20:39, n = rep(100, 20))
fun_y <- function(data, time) data[, age / 40]
fun_mortality <- function(data, period) data[, ifelse(age >= 30, 10, 0)]
fun_coming_of_age <- function(data, period) data.table(age = 20, n = 100)

result <- sim_social_change(
  periods = 5,
  data = data,
  fun_y = fun_y,
  fun_mortality = fun_mortality,
  fun_coming_of_age = fun_coming_of_age
)
print(result)
}

}
