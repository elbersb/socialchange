% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose_events.R
\name{decompose_events}
\alias{decompose_events}
\title{Decompose social change from discrete events}
\usage{
decompose_events(
  events,
  outcomes,
  formula,
  end_period = NULL,
  event_type = "event_type",
  fun = mean
)
}
\arguments{
\item{events}{data.frame with unit entry/exit events, must contain unit ID, time index, and event type columns}

\item{outcomes}{data.frame with outcome values by unit and time period}

\item{formula}{Formula specifying \code{Outcome ~ Unit + Time} structure}

\item{end_period}{Optional numeric end period; defaults to maximum event time}

\item{event_type}{Character name of column indicating event type (entry/exit)}

\item{fun}{Aggregation function to compute period-level outcomes (default: mean)}
}
\value{
S3 object of class \code{decompose_events} containing decomposition results
}
\description{
Event-based decomposition for populations where units enter and exit at specific time points.
Separates aggregate change into within-unit change and population replacement components.
}
\examples{
\donttest{
library(data.table)
events <- data.table(
  country = c("A", "B", "A"),
  year = c(1995, 2000, 2010),
  event_type = c("entry", "entry", "exit")
)
outcomes <- data.table(
  gdp = runif(50),
  country = rep(c("A", "B"), each = 25),
  year = rep(1995:2019, 2)
)
result <- decompose_events(events, outcomes, gdp ~ country + year)
print(result)
}

}
\seealso{
[decompose_aggregated()] for decomposing change in stacked cross-sectional data,
  [sim_social_change()] for forward simulation with fully specified demographic functions.
  Vignette: \code{vignette("gss_homosexuality", package = "socialchange")}.
}
